<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyShiftManager">

        <activity
            android:name=".Activities.Activity_Splash"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".Activities.Activity_Panel"
            android:exported="true"/>

        <activity
            android:name=".Activities.Activity_MakeProfile"
            android:exported="true"/>

        <activity
            android:name=".Activities.Activity_Login"
            android:exported="true"/>

        <activity
            android:name=".Activities.Activity_MakeWorkplace"
            android:exported="true"/>

        <activity
            android:name=".Activities.Activity_MakeShift"
            android:exported="true"/>
    </application>

</manifest>